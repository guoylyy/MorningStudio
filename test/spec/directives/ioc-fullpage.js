'use strict';

describe('Directive: iocFullpage', function () {

  // load the directive's module
  beforeEach(module('iocUiApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
